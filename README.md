# Install nginx proxy now
**Via curl**
```
sh -c "$(curl -fsSL https://raw.githubusercontent.com/clayton943/nginx/master/install.sh)"
```

**Via wget**
```
sh -c "$(wget https://raw.githubusercontent.com/clayton943/nginx/master/install.sh -O -)"
```
